apiVersion: v1
kind: Service
metadata:
  name: collabdocs-ws
  labels:
    {{- include "collabdocs.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: collabdocs-ws
  ports:
  - port: {{ .Values.websocket.port }}
    targetPort: {{ .Values.websocket.port }}
